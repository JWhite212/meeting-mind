# Audio capture and processing
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# Local speech-to-text (MLX Whisper — Apple Silicon GPU acceleration).
# Markers keep pip-compile resolvable on non-Apple-Silicon runners
# (e.g. GitHub's Linux x86_64 dependency-submission action); these
# wheels only exist for darwin/arm64. The transitives are listed
# explicitly because pip-tools 7.x doesn't always propagate the marker
# through dependency chains.
mlx-whisper>=0.4.0; sys_platform == "darwin" and platform_machine == "arm64"
mlx>=0.31; sys_platform == "darwin" and platform_machine == "arm64"
mlx-metal>=0.31; sys_platform == "darwin" and platform_machine == "arm64"

# LLM summarisation
anthropic>=0.40.0

# Notion integration
notion-client>=2.2.0

# Configuration
pyyaml>=6.0.1

# Utilities
python-slugify>=8.0.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0

# Database
aiosqlite>=0.21.0
sqlite-vec>=0.1.6

# Calendar integration (macOS EventKit — skipped on non-macOS platforms)
pyobjc-framework-EventKit>=10.0; sys_platform == "darwin"

# Semantic search (optional — vector search disabled without it)
sentence-transformers>=2.2.0

# ML-based speaker diarisation (optional — energy-based diarisation works without it)
# pyannote.audio>=3.1
